import React from "react";

export const ExchangeTerms = () => {
    return (
        <div className={'exchange-terms mt-[1em] text-[.85em] text-center text-white/[.6] font-[MontL] relative w-full'}>
            By using the site and creating an exchange, you agree to the FixedFloat's <a className={'text-[#018fdf] font-[MontBold]'}>Terms of Services</a> and <a className={'text-[#018fdf] font-[MontBold]'}>Privacy Policy</a>
        </div>
    )
}
